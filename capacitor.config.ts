import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.masiama.libri',
  appName: 'libri',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
